# Python Data Cleaning Sample

A sample script showing how I clean messy CSV/Excel exports using Python (pandas).

**What it fixes:** duplicate IDs, inconsistent date formats, mismatched 
capitalization, currency symbols, blank/missing values — while flagging 
anything genuinely invalid instead of silently guessing.

- `sample_messy_orders.csv` — raw, messy input data
- `clean_orders.py` — the cleaning script
- `sample_cleaned_orders.csv` — the cleaned output

Available for hire on Fiverr: https://www.fiverr.com/s/GzEGQWd
